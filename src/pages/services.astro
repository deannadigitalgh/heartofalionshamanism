---
import Layout from '../layouts/Layout.astro';
import Logo from '../components/Logo.astro';
import ContactForm from '../components/ContactForm.astro';
import { site, individualSessions as allIndividualSessions, workshops as allWorkshops, ceremonies, faqs } from '../config/site.js';

const seoTitle = `${site.name} | Shamanic Healing Services - Northern Georgia`;
const seoDescription = "Individual shamanic healing sessions, workshops, and training programs in Blue Ridge, Ellijay & Blairsville GA. Soul retrieval, energy work, learn to journey courses.";
---

<Layout title={seoTitle} seoDescription={seoDescription}>
	<!-- Hero Section -->
	<section class="hero">
		<div class="hero__content text-center">
			<h1 style="color: var(--color-earth-900); font-size: var(--text-5xl); margin-bottom: var(--spacing-5);">Shamanic Healing Services</h1>
			<p class="lead mb-6" style="opacity: 0.95;">
				Transformative healing sessions and workshops in Northern Georgia with {site.tagline}
			</p>
			<div class="hero__actions">
				<a href="#individual" class="btn btn--secondary btn--lg">Individual Sessions</a>
				<a href="/workshops" class="btn btn--outline btn--lg">Workshops</a>
			</div>
		</div>
	</section>

	<!-- Branding Logo Section -->
	<section class="section section--light" style="padding: var(--spacing-12) 0;">
		<div class="container text-center">
			<Logo variant="hero" />
			<p class="lead" style="max-width: 700px; margin: 0 auto;">
				Serving Northern Georgia with authentic shamanic healing, workshops, and training programs since 1995.
			</p>
		</div>
	</section>

	<!-- Individual Sessions Section -->
	<section id="individual" class="section">
		<div class="container">
			<h2 class="text-center mb-4" style="color: var(--color-primary-800); font-size: var(--text-3xl);">Individual Healing Sessions</h2>
			<p class="lead text-center mb-8" style="max-width: 800px; margin-left: auto; margin-right: auto;">
				Personalized shamanic healing tailored to your unique needs and intentions. Each session creates a sacred space for deep transformation and restoration.
			</p>

			<div class="grid grid--2" style="gap: var(--spacing-8);">
				{allIndividualSessions.map(session => (
					<div class="card service-card">
						<div class="service-header" style="display: flex; justify-content: space-between; align-items: flex-start; margin-bottom: var(--spacing-6);">
							<h3 style="color: var(--color-gold-500); font-size: var(--text-2xl); margin-bottom: 0;">{session.title}</h3>
							<div class="service-price" style="text-align: right;">
								<p style="font-weight: var(--font-semibold); color: var(--color-primary-800); font-size: var(--text-lg);">{session.price}</p>
								<p style="color: var(--color-text-muted);">{session.duration}</p>
							</div>
						</div>

						<p class="mb-4" style="font-size: var(--text-lg); line-height: 1.8;">
							{session.description}
						</p>

						<h4 style="color: var(--color-primary-800); font-size: var(--text-lg); margin-bottom: var(--spacing-3);">Includes:</h4>
						<ul class="service-list" style="list-style: none; display: grid; gap: var(--spacing-2); margin-bottom: var(--spacing-6);">
							{session.includes.map(item => (
								<li class="service-list__item" style="display: flex; align-items: center;">
									<span class="service-list__icon" style="color: var(--color-gold-500); margin-right: var(--spacing-3);">✓</span> {item}
								</li>
							))}
						</ul>

						<h4 style="color: var(--color-primary-800); font-size: var(--text-lg); margin-bottom: var(--spacing-3);">Ideal For:</h4>
						<p>{session.idealFor.join(" • ")}</p>

						<div class="mt-6 text-center">
							<a href="#contact" class="btn btn--primary">Book This Session</a>
						</div>
					</div>
				))}
			</div>
		</div>
	</section>

	<!-- Workshops Section -->
	<section id="workshops" class="section section--light">
		<div class="container">
			<h2 class="text-center mb-4" style="color: var(--color-primary-800); font-size: var(--text-3xl);">Workshops & Training</h2>
			<p class="lead text-center mb-8" style="max-width: 800px; margin-left: auto; margin-right: auto;">
				Transformative group experiences designed to awaken your inner wisdom and connect you with the healing power of shamanism.
			</p>

			<div class="grid grid--2" style="gap: var(--spacing-8);">
				{allWorkshops.map(workshop => (
					<div class="card workshop-card">
						<div class="service-header" style="display: flex; justify-content: space-between; align-items: flex-start; margin-bottom: var(--spacing-6);">
							<h3 style="color: var(--color-gold-500); font-size: var(--text-2xl); margin-bottom: 0;">{workshop.title}</h3>
							<div class="service-price" style="text-align: right;">
								<p style="font-weight: var(--font-semibold); color: var(--color-primary-800); font-size: var(--text-lg);">{workshop.price}</p>
								<p style="color: var(--color-text-muted);">{workshop.duration}</p>
							</div>
						</div>

						<p class="mb-4" style="font-size: var(--text-lg); line-height: 1.8;">
							{workshop.description}
						</p>

						{workshop.location && (
							<p class="mb-4"><strong>📍 {workshop.location}</strong></p>
						)}

						<h4 style="color: var(--color-primary-800); font-size: var(--text-lg); margin-bottom: var(--spacing-3);">Includes:</h4>
						<ul class="service-list" style="list-style: none; display: grid; gap: var(--spacing-2); margin-bottom: var(--spacing-6);">
							{workshop.includes.map(item => (
								<li class="service-list__item" style="display: flex; align-items: center;">
									<span class="service-list__icon" style="color: var(--color-gold-500); margin-right: var(--spacing-3);">✓</span> {item}
								</li>
							))}
						</ul>

						{workshop.nextDate && (
							<p class="info-box" style="background-color: var(--color-bg-secondary); padding: var(--spacing-4); border-radius: var(--radius-md); margin-bottom: var(--spacing-6);">
								<strong>📅 Next Date:</strong> {workshop.nextDate}
							</p>
						)}

						{workshop.registration && (
							<p class="info-box" style="background-color: #E8F4FD; padding: var(--spacing-4); border-radius: var(--radius-md); margin-bottom: var(--spacing-6);">
								<strong>📞 Registration:</strong> {workshop.registration}
							</p>
						)}

						{workshop.note && (
							<p class="info-box" style="background-color: #FFF3CD; padding: var(--spacing-4); border-radius: var(--radius-md); margin-bottom: var(--spacing-6);">
								<strong>⚠️ Note:</strong> {workshop.note}
							</p>
						)}

						<div class="mt-6 text-center">
							<a href="#contact" class="btn btn--primary">Register for This Workshop</a>
						</div>
					</div>
				))}
			</div>
		</div>
	</section>

	<!-- Ceremonies Section -->
	<section id="ceremonies" class="section">
		<div class="container">
			<h2 class="text-center mb-8" style="color: var(--color-primary-800); font-size: var(--text-3xl);">Special Ceremonies</h2>

			<div class="grid grid--3" style="gap: var(--spacing-8);">
				{ceremonies.map(ceremony => (
					<div class="card card--icon ceremony-card">
						<h3 style="color: var(--color-gold-500); font-size: var(--text-xl); margin-bottom: var(--spacing-4);">{ceremony.title}</h3>
						<p style="line-height: 1.6; margin-bottom: var(--spacing-2);">{ceremony.description}</p>
						<p style="font-weight: var(--font-semibold); color: var(--color-primary-800); margin-bottom: var(--spacing-2);">{ceremony.frequency}</p>
						<p style="color: var(--color-gold-500); font-size: var(--text-lg);">{ceremony.price}</p>
					</div>
				))}
			</div>
		</div>
	</section>

	<!-- FAQ Section -->
	<section class="section section--light">
		<div class="container" style="max-width: 900px;">
			<h2 class="text-center mb-8" style="color: var(--color-primary-800); font-size: var(--text-3xl);">Frequently Asked Questions</h2>

			<div class="faq-list">
				{faqs.general.map(faq => (
					<div class="faq-item">
						<h3 class="faq-question">{faq.question}</h3>
						<p class="faq-answer">{faq.answer}</p>
					</div>
				))}
			</div>
		</div>
	</section>

	<!-- Contact Section -->
	<section id="contact" class="section section--dark">
		<div class="container" style="max-width: 900px;">
			<h2 class="text-center mb-4" style="color: var(--color-gold-500); font-size: var(--text-3xl);">Book Your Session or Workshop</h2>
			<p class="lead text-center mb-8" style="opacity: 0.95;">
				Ready to begin your shamanic journey? Contact {site.tagline.split(',')[0]} today to schedule your session or register for a workshop.
			</p>

			<div class="grid grid--2" style="gap: var(--spacing-8); max-width: 600px; margin: 0 auto;">
				<ContactForm 
					title="Send {site.tagline.split(',')[0]} a Message"
					showServiceDropdown={true}
					buttonText="Send Message"
				/>

				<div class="contact-info-card" style="text-align: center;">
					<p style="font-size: var(--text-lg); margin-bottom: var(--spacing-3);"><strong>📞 Phone/Text:</strong></p>
					<a href={`tel:${site.phone}`} style="font-size: var(--text-2xl); color: var(--color-gold-500); font-weight: var(--font-bold);">{site.phone}</a>

					<p style="font-size: var(--text-lg); margin-top: var(--spacing-6); margin-bottom: var(--spacing-2);"><strong>✉️ Email:</strong></p>
					<a href={`mailto:${site.email}`} style="font-size: var(--text-lg); color: var(--color-gold-500);">{site.email}</a>
				</div>
			</div>
		</div>
	</section>

	<!-- Schema Markup -->
	<script type="application/ld+json">
		{
			"@context": "https://schema.org",
			"@type": "WebPage",
			"name": `${site.name} - Services`,
			"description": "Individual shamanic healing sessions, workshops, and training programs in Northern Georgia with Deanna Stennett, L.Ac., MA.",
			"url": "https://heartofalionshamanism.com/services"
		}
	</script>
</Layout>
